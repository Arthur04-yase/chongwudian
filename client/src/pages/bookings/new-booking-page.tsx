import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
export default function NewBookingPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>➕ 新增预约</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-sm text-muted-foreground">待 Task 4.1 实现</p>
      </CardContent>
    </Card>
  )
}
